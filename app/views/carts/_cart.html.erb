<% unless cart.line_items.empty? %>
    <article>

          <% if notice %>
            <aside id="notice"> <%= notice %> 
          <% end %>

          <h6>You have Shoped </h6>
          
          <table>
              <%= render(cart.line_items) %>  
              <tfoot>
                <tr>
                  <th colspan="2">Total:</th>   
                  <td class="price"><%= number_to_currency(cart.total_price) %>  </td>
                </tr>
              </tfoot>
          </table>
        
          <div class='actions'>
              <%= button_to "Empty Cart", @cart ,method: :delete, data: {confirm: "Arre you sure..."}   %>
              <%= button_to "Checkout",   new_order_path, 
                             method: :get,
                             class: 'checkout'%>

          </div>
    </article>
<% end %>